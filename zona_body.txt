zonas-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(210px,1fr));gap:16px;position:relative;z-index:1;}
.zona-card{
  background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.08);
  border-radius:14px;padding:22px;display:flex;align-items:center;gap:14px;
  transition:all .3s;cursor:default;
  position:relative;overflow:hidden;
}
.zona-card::before{
  content:'';position:absolute;left:0;top:0;bottom:0;width:2px;
  background:linear-gradient(180deg,var(--blue-500),var(--cyan-500));
  transform:scaleY(0);transform-origin:top;transition:transform .35s;
}
.zona-card:hover{background:rgba(11,21,237,.15);border-color:rgba(0,212,255,.25);transform:translateY(-3px);}
.zona-card:hover::before{transform:scaleY(1);}
.zona-ico{font-size:1.7rem;flex-shrink:0;}
.zona-card h4{font-size:.88rem;color:#fff;font-family:'Montserrat',sans-serif;font-weight:700;}
.zona-card p{font-size:.72rem;color:rgba(255,255,255,.5);margin-top:2px;}
.zona-cta{
  grid-column:span 1;
  background:linear-gradient(135deg,rgba(0,212,255,.12),rgba(11,21,237,.15));
  border-color:rgba(0,212,255,.25);
}

/* ═══════════════════════════════════════════════ TESTIMONIOS */
.testimonios{background:var(--gray-50);}
.testim-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:22px;}
.testim-card{
  background:#fff;border-radius:18px;padding:28px 24px;
  border:1px solid rgba(11,21,237,.06);
  box-shadow:0 4px 24px rgba(7,12,71,.06);
  transition:all .3s;position:relative;overflow:hidden;
}
.testim-card::before{
  content:'"';
  position:absolute;top:14px;right:20px;
  font-size:6rem;font-weight:900;line-height:1;
  color:var(--blue-100);font-family:'Montserrat',sans-serif;
  pointer-events:none;
}
.testim-card:hover{transform:translateY(-5px);box-shadow:0 16px 40px rgba(11,21,237,.1);}
.testim-stars{display:flex;gap:3px;margin-bottom:14px;}
.testim-stars span{color:var(--orange);font-size:1rem;}
.testim-text{font-size:.88rem;color:var(--gray-700);line-height:1.7;margin-bottom:18px;font-style:italic;}
.testim-author{display:flex;align-items:center;gap:12px;}
.testim-avatar{
  width:42px;height:42px;border-radius:50%;
  background:linear-gradient(135deg,var(--blue-500),var(--cyan-500));
  display:flex;align-items:center;justify-content:center;
  color:#fff;font-weight:700;font-family:'Montserrat',sans-serif;font-size:.9rem;
}
.testim-name{font-size:.85rem;font-weight:700;color:var(--navy-900);}
.testim-org{font-size:.75rem;color:var(--gray-400);}

/* ═══════════════════════════════════════════════ CONTACTO */
.contacto{background:#fff;position:relative;overflow:hidden;}
.contacto::before{
  content:'';position:absolute;
  top:0;right:0;width:50%;height:100%;
  background:var(--gray-50);z-index:0;
}
.contacto-wrap{position:relative;z-index:1;}
.contacto-grid{display:grid;grid-template-columns:1fr 1fr;min-height:600px;}
.contacto-left{
  background:linear-gradient(150deg,var(--navy-950) 0%,var(--navy-800) 60%,var(--blue-600) 100%);
  padding:64px 52px;position:relativ